using UnityEngine;

public abstract class AbstractShubEnnemyLogic : MonoBehaviour
{

    // ennemy actual state.
    public int ennemyStatus = Status.NONE;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
